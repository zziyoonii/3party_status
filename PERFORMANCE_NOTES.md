# 대시보드 성능 최적화 가이드

## 1분 단위 업데이트 시 우려사항 및 해결책

### 우려사항

#### 1. 데이터베이스 부하
- **문제**: 1분마다 여러 쿼리가 실행되어 DB 부하 증가
- **영향**: 
  - 모니터링 주기가 30초인 경우, 1분에 6개 레코드 생성 (3개 provider × 2회)
  - 시간이 지날수록 데이터베이스 크기 증가
  - 인덱스가 없으면 쿼리 성능 저하

#### 2. 네트워크 트래픽
- **문제**: 1분마다 여러 API 호출 발생
- **영향**:
  - 대시보드 열어둔 사용자 수 × API 호출 수 = 총 트래픽
  - 모바일 환경에서 배터리 소모

#### 3. 브라우저 성능
- **문제**: 1분마다 DOM 업데이트 및 차트 리렌더링
- **영향**:
  - 메모리 사용량 증가
  - 오래된 브라우저에서 성능 저하 가능

#### 4. 데이터베이스 크기 증가
- **문제**: 시간이 지날수록 레코드 수 증가
- **계산**:
  - 3개 provider × 2회/분 × 60분 × 24시간 = 8,640 레코드/일
  - 1개 레코드 약 500 bytes → 약 4.3 MB/일
  - 1년 후 약 1.6 GB

### 해결책

#### ✅ 적용된 최적화

1. **시간 필터링 추가**
   - 최근 모니터링 기록: 최근 1시간만 조회
   - 불필요한 오래된 데이터 조회 방지

2. **인덱스 추가**
   - `timestamp` 필드에 인덱스 추가 (시간 기반 쿼리 성능 향상)
   - `provider` 필드에 인덱스 추가 (provider 필터링 성능 향상)

3. **Limit 제한**
   - 최대 50개 레코드만 조회
   - 페이지네이션으로 추가 데이터 로드 가능

#### 🔄 추가 권장사항

1. **데이터 정리 작업 (Cleanup)**
   ```javascript
   // 7일 이상 된 레코드 자동 삭제
   // scheduler.js에 추가 권장
   async cleanupOldRecords() {
     const cutoffDate = new Date(Date.now() - 7 * 24 * 60 * 60 * 1000);
     const deletedCount = await MonitoringRecord.destroy({
       where: {
         timestamp: {
           [Op.lt]: cutoffDate,
         },
       },
     });
     console.log(`✓ ${deletedCount}개의 오래된 레코드가 정리되었습니다.`);
   }
   ```

2. **캐싱 고려**
   - 통계 데이터는 30초 캐시
   - 최근 기록은 1분 캐시
   - Redis 또는 메모리 캐시 사용

3. **조건부 업데이트**
   - 탭이 비활성화된 경우 업데이트 중단
   - 네트워크 상태 확인 후 업데이트

4. **데이터베이스 아카이빙**
   - 오래된 데이터는 별도 테이블로 이동
   - 통계용 집계 테이블 생성

### 모니터링 지표

다음 지표를 모니터링하세요:
- 데이터베이스 크기
- 쿼리 실행 시간
- API 응답 시간
- 브라우저 메모리 사용량

### 권장 설정

```env
# .env 파일
MONITORING_INTERVAL=30  # 모니터링 주기 (초)
# 대시보드 업데이트: 1분 (프론트엔드)
# 데이터 정리: 24시간마다 (7일 이상 된 데이터)
```
